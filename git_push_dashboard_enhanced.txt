#!/bin/bash
# Git commands to save current dashboard enhancements

# 1. First, check current status
git status

# 2. Add all changes
git add .

# 3. Create a new branch for the enhanced dashboard
git checkout -b dashboard-enhanced-features

# 4. Commit with descriptive message
git commit -m "✨ Enhanced Dashboard with New Features

- Added price ticker with BTC price, % change, next block time, and fee rate
- Added stats grid with Bitcoin, Lightning, Stablecoins, and Network status boxes
- Added wallet selector dropdown with multiple wallet types
- Added quick actions bar (Copy Address, Show QR, View on Explorer, Export xPub, Manage UTXOs)
- Added wallet health indicator showing security score, backup, and 2FA status
- Added recent activity feed showing last 5 transactions
- Added keyboard shortcut hint
- Fixed navigation from wallet creation to dashboard (was going to wallet-details)
- Fixed MOOSH mode styling - all frames now turn green
- Added blinking active indicator in terminal header
- Improved layout with proper spacing between sections"

# 5. Push the new branch to remote
git push -u origin dashboard-enhanced-features

# If you get an error about remote not existing, use:
# git remote add origin <your-repo-url>
# Then run the push command again

# 6. To switch back to your previous branch later:
# git checkout theme-toggle-fix

# 7. To see all branches:
# git branch -a